#include<bits/stdc++.h>
typedef long long int ll;
using namespace std;
const ll mx = INT_MAX;
const ll mn = INT_MIN;

/* Vogoban please code ta accept korai dew >.< */

int main()
{
    int t;
    cin>>t;
    for (ll i = 1 ; i <= t ; i++)
    {
        double n,c=0;
        cin >> n;
        double pi=2 * acos (0.0);
        printf("Case %d: %.2lf\n", i, 4*n*n-pi*n*n);
    }
}
