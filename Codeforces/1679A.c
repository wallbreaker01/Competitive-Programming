#include<stdio.h>
int main()
{
    int t,n,x,y;
    scanf("%d",&t);
    while(t--)
    {
         scanf("%d",&n);
         if(n%4==0)
         {
             x=n/4
         }
         if(n%6==0)
         {
             y=
         }
    }
}
