#include<bits/stdc++.h>
#include<iostream>
#include<string.h>
#include<math.h>
typedef long long int ll;
using namespace std;


int main()
{
    ll n,k;
    cin >> n >> k ;
    ll s[n];
    ll c=1,sum=0;
    for (ll i = 1 ; i <= n ; i++) 
    {
        cini>>s[i];
        sum=sum+s[i];
    }
    if(c!=k) c++;
    else
    {
        sum=sum-s[i-k];
        if()
    }

    
}