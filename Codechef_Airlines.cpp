#include<bits/stdc++.h>
typedef long long int ll;
using namespace std;
const ll mx = INT_MAX;
const ll mn = INT_MIN;

/* Vogoban please code ta accept korai dew >.< */

void solve()
{
    ll a,b,c;
    cin>>a>>b>>c;
    a=10*a;
    ll sum;
    if(b>a) sum=a*c;
    else sum=b*c;
    cout<<sum<<endl;
     


}

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
}
